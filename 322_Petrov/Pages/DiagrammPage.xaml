<Page x:Class="_322_Petrov.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_322_Petrov.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DiagrammPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" HorizontalAlignment="Left">
            <ComboBox x:Name="CmbUser" 
  DisplayMemberPath="FIO"
  Width="200" 
  Margin="5"
  SelectionChanged="UpdateChart"/>

            <ComboBox x:Name="CmbDiagram" 
  Width="150" 
  Margin="5"
  SelectionChanged="UpdateChart"/>

            <Button x:Name="BtnExportExcel" 
Content="Экспорт в Excel" 
Width="120" 
Margin="5"
Click="BtnExportExcel_Click"/>

            <Button x:Name="BtnExportWord" 
Content="Экспорт в Word" 
Width="120" 
Margin="5"
Click="BtnExportWord_Click"/>

        </StackPanel>
        <WindowsFormsHost Grid.Row="1" Margin="5">
            <charts:Chart x:Name="ChartPayments">
                <charts:Chart.Legends>
                    <charts:Legend/>
                </charts:Chart.Legends>
            </charts:Chart>
        </WindowsFormsHost>
        <WindowsFormsHost HorizontalAlignment="Left" Height="100" Margin="-188,-102,0,0" VerticalAlignment="Top" Width="100"/>
    </Grid>
</Page>
